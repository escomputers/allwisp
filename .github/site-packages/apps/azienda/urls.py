from django.contrib import admin
from django.urls import path
from .import views
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url


    # # # CUSTOMERS
urlpatterns = [
    path('list', views.lista_aziende, name='lista_aziende'),
	path('<int:ragionesociale>/update', views.aggiorna_azienda, name='aggiorna_azienda'),
    path('<int:ragionesociale>/delete', views.delete_azienda, name='delete_azienda'),
    path('new', views.aggiungi_azienda, name='aggiungi_azienda')
]