from django.contrib import admin
from django.urls import path
from .import views,magazzino
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url


    # # # CUSTOMERS
urlpatterns = [
    path('listaaziende', views.azienda_list, name='aziendalist_list'),
	path('<int:ragionesociale>', views.update_azienda, name='update_azienda'),
    path('<int:ragionesociale>/delete', views.delete_azienda, name='delete_azienda'),
    path('new_azienda', views.nuovazienda, name='nuovazienda'),
    path('magazzino', magazzino.riepilogo_magazzino, name='magazzino'),
]
