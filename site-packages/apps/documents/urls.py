from django.urls import path, re_path
from django.contrib import admin
from .import views
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url

urlpatterns = [

    path('new', views.new_document, name='new_document'),
    path('all', views.all_documents, name='all_documents'),
    path('draft', views.draft_documents, name='draft_documents'),
    path('paid', views.paid_documents, name='paid_documents'),
    path('unpaid', views.unpaid_documents, name='unpaid_documents'),
    path('document/<str:url>', views.document, name='document'),
    #path('search', views.search_document, name='search_document'),
    path('<int:document_id>/update', views.update_document, name='update_document'),
    path('<int:document_id>/print', views.print_document, name='print_document'),
    path('<int:document_id>/delete', views.delete_document, name='delete_document'),
]
