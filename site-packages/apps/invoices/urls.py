from django.urls import path, re_path
from django.contrib import admin
from .import views
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url

urlpatterns = [

    # # # INVOICES

    path('new', views.new_invoice, name='new_invoice'),
    path('all', views.all_invoices, name='all_invoices'),
    path('draft', views.draft_invoices, name='draft_invoices'),
    path('paid', views.paid_invoices, name='paid_invoices'),
    path('unpaid', views.unpaid_invoices, name='unpaid_invoices'),
    path('invoice/<str:url>', views.invoice, name='invoice'),
    #path('search', views.search_invoice, name='search_invoice'),
    path('<int:invoice_id>/update', views.update_invoice, name='update_invoice'),
    path('<int:invoice_id>/print', views.print_invoice, name='print_invoice'),
    path('<int:invoice_id>/delete', views.delete_invoice, name='delete_invoice'),
]
