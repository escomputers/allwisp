from django.urls import path, re_path
from django.contrib import admin
from .views import views,crea_fatture,fatture,liste_documenti
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url

urlpatterns = [

    # # # INVOICES

    path('new', crea_fatture.new_invoice, name='new_invoice'),
    path('all_invoices', liste_documenti.lista_fatture, name='all_invoices'),
    path('all_note_credit', liste_documenti.lista_nota_credito, name='all_note_credit'),
    path('all_draft', liste_documenti.lista_preventivi, name='all_draft'),
    path('invoice/<str:invoice_id>', fatture.view_invoice, name='update_status'),
    #path('notadicredito/<str:invoice_id>', views.nota_di_credito, name='nota_di_credito'),
    path('<str:linkviewsdocumenti_fiscali>', fatture.invoice, name='invoice'),
    #path('search', views.search_invoice, name='search_invoice'),
    path('<str:invoice_id>/update', fatture.view_invoice, name='view_invoice'),
    path('<int:invoice_id>/print', views.print_invoice, name='print_invoice'),
    path('success/<str:document_legal>', fatture.success_legal_document, name='success_document_legal'),
    path('error/<str:document_legal>', fatture.error_legal_document, name='error_document_legal'),
    path('ajax/ricercainvociecliente', crea_fatture.ricercainvociecliente, name='ricercainvociecliente'),
    
]
