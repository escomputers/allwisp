from django.urls import path
from django.contrib import admin
from .import views
from django.conf import settings
from django.conf.urls.static import static
from django.conf.urls import url



urlpatterns = [
    
    # # # INVOICE EXPENSES

    path('<int:document_id>/expenses/add', views.add_document_expense, name='add_document_expense'),
    path('<int:document_id>/expenses/<int:expense_id>/delete', views.delete_document_expense, name='delete_document_expense'),
   
]